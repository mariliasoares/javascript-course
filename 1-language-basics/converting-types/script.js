// ************ BEST PRACTICES *************
// whatever you get from a user, from a HTML input in JS code will ALWAYS be a string.
// to convert to a number instead of string concatenation -> mathematical equations

//take a string as an input and convert text to a number without decimal parts (parseFloat())
//parseInt();

//if a number without decimal places, can be used +userInput

//to convert a number to a string
//result.toString();

//shortcuts
// instead   currentResult = currentResult + enteredNumber;
// do        currentResult += enteredNumber;

// a semicolon is used after {} if the {} are on the right side of the equal sign!